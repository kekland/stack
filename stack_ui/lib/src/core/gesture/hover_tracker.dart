import 'package:flutter/widgets.dart';

class HoverTracker extends StatelessWidget {
  const HoverTracker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

