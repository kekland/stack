name: stack_ui
description: My own toolkit for building platform-aware Flutter UIs.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.21.3+1
  signals: ^6.3.0
  signals_flutter: ^6.3.0
  signals_hooks: ^0.4.0
  
  # utils
  collection: ^1.19.1
  email_validator: ^3.0.0
  oklch: ^1.0.0
  vector_math: ^2.2.0

  stack: 
    git:
      url: https://github.com/kekland/stack
      path: stack
